version: 0.2

phase:
  install:
    commands:
       -echo Installing NGINX
       -sudo apt-get update
       -sudo apt-get install nginx -y
  build:
    commands:
       -echo Build Started on `date`
  post_build:
    commands:
       -echo Configuring NGINX

artifacts:
   files:
     -'**/*'